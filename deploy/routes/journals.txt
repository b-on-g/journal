# Журналы, которые попадают в поисковую выдачу.
#
# Одна строка — один журнал. Можно писать голую ссылку на ленд:
#
#     hFqK8s_2nvY0pDqXWmA1
#
# либо готовый маршрут, если нужен не корень журнала:
#
#     author=hFqK8s_2nvY0pDqXWmA1
#
# Пустые строки и всё после # игнорируются.
#
# Зачем список руками. Глобального реестра журналов в Гипер Базе нет: ссылка
# на журнал лежит в домашнем ленде автора, а домашний ленд читает только он
# сам. То есть узнать «какие вообще есть журналы» неоткуда — их надо назвать.
# Побочный плюс: индексируется ровно то, что решили индексировать, а не всё,
# до чего дотянулся краулер.
#
# Ссылку на свой журнал видно в адресной строке: часть после author= на
# https://b-on-g.github.io/journal/author=<вот это>
#
# Пока файл пуст, пререндерится только главная. Это не ошибка сборки.
